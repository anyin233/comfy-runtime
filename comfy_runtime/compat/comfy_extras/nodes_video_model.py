"""Import-compat stub for `comfy_extras.nodes_video_model`.

Created so cross-module imports inside ComfyUI's own `comfy_extras/`
files (e.g. `from comfy_extras.nodes_video_model import X`) succeed when
loaded through the compat shim.  Phase 5 will populate the actual
node classes that workflows depend on.
"""
